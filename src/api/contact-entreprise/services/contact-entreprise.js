'use strict';

/**
 * contact-entreprise service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::contact-entreprise.contact-entreprise');
